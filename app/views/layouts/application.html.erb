<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>

        <link href='http://fonts.googleapis.com/css?family=Lora' rel='stylesheet' type='text/css'>
        <link href='http://fonts.googleapis.com/css?family=Lato' rel='stylesheet' type='text/css'>
        
        <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
        <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
        <%= csrf_meta_tags %>
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <h1>
                        <%= title %>
                        <small>
                            <%= sub_title %>
                        </small>
                    </h1>
                    <ul class="list-inline"> 
                        <li><%= link_to "home", root_url %></li>
                        <li><%= link_to "archive", archive_path %></li>
                        <li><%= link_to "tags", tags_path %></li>
                        <li><%= link_to "about", about_path %></li>

                        <% if !current_user.nil? %>
                            <li><%= link_to("new post", { controller: "posts", action: "new" }) %></li>
                            <li><%= link_to("sign out", logout_path) %></li>
                        <% end %>
                    </ul>
                    <hr>
                </div>
            </div>
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <%= render "layouts/alert" %>
                </div>
            </div>
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <%= yield %>
                </div>
            </div>
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <hr>
                    <footer>
                    &copy; 2014 Author
                    <span class="pull-right text-muted">This blog uses <%= link_to "Lilliputian", "http://www.bitbucket.org/argelius/lilliputian" %>, a tiny blog application written in Rails.</span>
                    </footer>
                </div>
            </div>
        </body>
    </html>
